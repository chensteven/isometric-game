using System.Collections;

public static class Enums {

	public enum Tool {Select, Move, Build};
	public enum CraftableItems {GrassPatch4x4, BasicWall};
	public enum ArrowDirection {Left,Right,Up,Down};
}
